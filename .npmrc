# BlockNote pulls Mantine, whose peer-dependency graph npm can't auto-resolve
# cleanly. legacy-peer-deps lets `npm install`/`npm ci` (incl. the Docker build)
# proceed with the locked versions instead of erroring on the peer conflict.
legacy-peer-deps=true
